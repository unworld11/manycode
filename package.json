{
  "name": "ccshare",
  "version": "0.1.0",
  "description": "Multiplayer Claude Code - share your live session with a code, like AirDrop",
  "license": "MIT",
  "bin": {
    "ccshare": "bin/ccshare.js"
  },
  "scripts": {
    "test": "node test/smoke.js && node test/edges.js",
    "postinstall": "chmod +x node_modules/node-pty/prebuilds/*/spawn-helper 2>/dev/null || true"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "cloudflared": "^0.7.1"
  }
}
